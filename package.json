{
  "name": "work-hours-tracker-web",
  "version": "1.0.0",
  "description": "Hebrew work and hours tracker with cloud sync and local persistence",
  "private": true,
  "scripts": {
    "build": "mkdir -p public && cp index.html public/ 2>/dev/null || true",
    "start": "npx serve public"
  }
}
